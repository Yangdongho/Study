IO
다른 프로그램과 데이터를 주고 받는 것 스트림을 이용한다
인 - input stream
아웃 - 아웃 stream

Stream 종류 - 바이트,문자
소스에 데이터를 직접 쓰거나 읽어오는 Stream = 데이터 싱크 Stream
Stream 을 읽거나 쓰기 편하게 바꿔주는 Stream = 데이터 처리 stream

	in = new DataInputStream(new BufferedInputStream(new FileInputStream("DADA.txt")));
	out.writeInt(100);
	out.writeDouble(1.1);;
	out.writeUTF("Hello");
	
	
	out = new ObjectOutputStream(new BufferedOutputStream(new FileOutputStream("stu.dat")));
	out.writeObject(s); 
	객체를 저장하고자 할때는 직렬화 처리가 필요하다 implements Serializable를 보내고자하는 클래스에 쓰기 
	transient 해당 변수는 직렬화에서 제외
	객체를 파일에 저장했다가 다시 불러오면 기본값으로 지정된다.