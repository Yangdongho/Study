vector , enumeration 해보기
Thread - 프로세스내에서의 명령 실행 단위
프로세스 - 소프트웨어의 실행단위
메인쓰레드는 개발자가 만들지 않고 알아서 생성됨
멀티 쓰레드 - 여러 프로그램을 동시에 실행, 각각 다른 프로그램이 실행되는 것이기때문에 데이터의 공유에 불편함이 있다.

thread 클래스의 start() 메서드 호출로 스레드 생성
1. 스레드 클래스를 상속받아 run()을 오버라이딩
Mythread thread1 = new Mythread();//파생쓰레드
		thread1.start(); //쓰레드가 가지고있는 run()메서드를 실행한다	



2. runnable 인터페이스 구현해서 스레드 실행,다중 상속이 불가능하기 때문에 자주 사용되는 방식이다.
 MyRunnable th = new MyRunnable();
		Thread thread = new Thread(th); 
		thread.start();

쓰레드의 상태값 받아오기 getState();
 
쓰레드가 동시에 변수에 접근을 막아야 한다. -> public synchronized int getBalance();

wait(); notify();
try{
	wait()
}
	catch(InterruptedException e){
}

notifyAll();	- 
notify(); 		- 






 



